# Configuração para deploy no Render.com
# https://render.com/docs/yaml-spec

services:
  # Web Service (Django)
  - type: web
    name: lista-presentes
    env: python
    region: oregon # ou ohio, frankfurt, singapore
    plan: free # ou starter, standard, pro
    branch: master # branch para deploy automático

    # Build - Usar script para garantir DJANGO_SETTINGS_MODULE correto
    buildCommand: bash build.sh

    # Start - Usa gunicorn.conf.py para configurações otimizadas
    # Veja gunicorn.conf.py para detalhes de workers, threads, timeout, etc.
    startCommand: |
      gunicorn --config gunicorn.conf.py lista_presentes.wsgi:application

    # Variáveis de ambiente
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

      - key: DEBUG
        value: false

      - key: DJANGO_SETTINGS_MODULE
        value: lista_presentes.settings

      - key: SECRET_KEY
        generateValue: true

      - key: ALLOWED_HOSTS
        value: .onrender.com,lista-presentes-0hbp.onrender.com

      # ==============================================================================
      # Supabase PostgreSQL Database
      # ==============================================================================
      # IMPORTANTE: Defina estas variáveis no Dashboard do Render.com
      # Database URL format: postgresql://postgres:senha@db.xxxxx.supabase.co:5432/postgres

      - key: DATABASE_URL
        sync: false  # Definir manualmente no dashboard com a connection string do Supabase

      - key: SUPABASE_URL
        sync: false  # URL do projeto Supabase (opcional, para uso futuro)

      - key: SUPABASE_KEY
        sync: false  # Chave pública do Supabase (opcional, para uso futuro)

      # ==============================================================================
      # API Keys
      # ==============================================================================
      - key: ANTHROPIC_API_KEY
        sync: false  # Definir manualmente no dashboard

      - key: OPENAI_API_KEY
        sync: false  # Definir manualmente no dashboard

      - key: GEMINI_API_KEY
        sync: false  # Definir manualmente no dashboard

      # Otimizações para Free Tier
      - key: WEB_CONCURRENCY
        value: 1  # Número de workers (Free: 1, Starter+: 2-4)

      - key: GUNICORN_TIMEOUT
        value: 120  # Timeout em segundos

      - key: MAX_REQUESTS
        value: 1000  # Reiniciar worker após N requests (previne memory leaks)

    # Health check
    healthCheckPath: /health/

# ==============================================================================
# Database - Usando Supabase PostgreSQL Externo
# ==============================================================================
# Removemos a seção databases porque agora usamos Supabase ao invés do
# PostgreSQL gerenciado pelo Render. Isso oferece:
#
# ✅ Vantagens do Supabase:
#    - Free tier mais generoso (500MB vs 256MB do Render Free)
#    - Backup automático
#    - Interface web para gerenciar dados
#    - API REST automática
#    - Realtime subscriptions
#    - Storage para arquivos
#    - Authentication integrado
#
# 📝 Para configurar, adicione estas variáveis no Dashboard do Render:
#    DATABASE_URL=postgresql://postgres:senha@db.xxxxx.supabase.co:5432/postgres
#    SUPABASE_URL=https://xxxxx.supabase.co
#    SUPABASE_KEY=sua_chave_publica
# ==============================================================================
