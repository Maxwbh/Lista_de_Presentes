<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
  <!-- Background -->
  <rect width="128" height="128" fill="#667eea"/>

  <!-- Gift box -->
  <g transform="translate(64, 64)">
    <!-- Box body -->
    <rect x="-32" y="-16" width="64" height="32" fill="#ffffff" rx="8"/>

    <!-- Box lid -->
    <rect x="-32" y="-32" width="64" height="16" fill="#f5f5f5" rx="8"/>

    <!-- Ribbon vertical -->
    <rect x="-5" y="-32" width="10" height="64" fill="#764ba2" rx="4"/>

    <!-- Ribbon horizontal -->
    <rect x="-32" y="-5" width="64" height="10" fill="#764ba2" rx="4"/>

    <!-- Bow -->
    <circle cx="-10" cy="-32" r="8" fill="#f093fb"/>
    <circle cx="10" cy="-32" r="8" fill="#f093fb"/>
    <circle cx="0" cy="-32" r="6" fill="#f5576c"/>
  </g>

  <!-- Text (optional for larger icons) -->
  
</svg>